\section{Results}

The B3/B4 closure used the consolidated B1+B2 ranking to build a final publication table (REP-01), plus significance tests (STAT-02) over the full-scale B2 raw data.

\subsection{Family-Level Summary}

Table~\ref{tab:b3_rep01_summary} summarizes average performance across all scenarios and missing-ratio levels. The best method within each family is reported in the rightmost column.

\begin{table}[t]
\centering
\caption{Final REP-01 summary (B1+B2 consolidated, phase \ResPhase).}
\label{tab:b3_rep01_summary}
\begin{tabular}{lcccc}
	oprule
Group & MAE $\downarrow$ & DTW $\downarrow$ & RMSE $\downarrow$ & Top method \\
\midrule
Baseline   & \ResBaselineMAEFull & \ResBaselineDTWFull & \ResBaselineRMSEFull & \ResBaselineTop \\
TV/Time    & \ResTVMAEFull       & \ResTVDTWFull       & \ResTVRMSEFull       & \ResTVTop       \\
GSP        & \ResGSPMAEFull      & \ResGSPDTWFull      & \ResGSPRMSEFull      & \ResGSPTop      \\
\bottomrule
\end{tabular}
\end{table}

The family-level summary reveals a performance crossover: the Baseline family achieves the lowest average MAE (\ResBaselineMAE) and RMSE (\ResBaselineRMSE), while the TV/Time family achieves the lowest average DTW (\ResTVDTW). This crossover has a practical implication: the best family depends on how reconstruction quality is defined. If pointwise amplitude fidelity is the priority (e.g., BCI feature extraction), baseline and GSP methods are competitive. If temporal dynamics must be preserved (e.g., event-related analysis), TV/time methods offer a clear advantage.

\subsection{Statistical Significance (STAT-02)}

STAT-02 tests \emph{reject $H_0$} for the key family contrast TV/time vs.\ instant on both MAE ($p = $\StatFamilyMAEpval) and DTW ($p = $\StatFamilyDTWpval). This confirms that family-level differences are not explained by noise alone under the current protocol.

At the method level, the contrast between TRSS and BGSRP is significant in MAE ($p = $\StatTRSSBGSRPpval), while TRSS vs.\ \iterid{gsmooth} fails to reject $H_0$ ($p = $\StatTRSSGSPpval). This supports a scenario-aware interpretation: there is a real difference between families, but within the GSP and TV/time families the top methods are not easily separable by MAE alone.

\subsection{Scenario-Dependent Behavior}

Full per-scenario rankings are available in \iterid{results/b2_publication_topk_by_family_scenario.csv}. Across the \ResNScenarios~scenarios (\ResScenariosNames), the relative ranking of methods changes depending on the region and level of missing data (\ResMissingLevels). This sensitivity is expected: frontal electrodes share different functional correlations than occipital ones, so a graph constructed from spatial proximity may not capture the dominant structure in all regions.

\subsection{BGSRP Status}

The BGSRP method is currently treated as a \BGSRPStatus. The residual gap is quantified in \iterid{results/b2_publication_bgsrp_gap_residual.csv} and classified as \emph{accepted for this stage}. BGSRP results should be interpreted accordingly until full cross-stack equivalence is established.

\subsection{Optuna Multi-Dataset Validation}

Following the initial phase, we conducted an extensive hyperparameter optimization using Optuna across three datasets (PhysioNet, MNE Sample, BCI IV 2a). This phase evaluated methods under optimized configurations to establish their true theoretical potential.

\subsubsection{Overall Performance Crossover}
The results under optimal hyperparameters demonstrate a paradigm shift. TRSS achieved an extraordinary MAE of \ResTVMAE\ and an SNR of \ResTVSNR~dB, conclusively outperforming the geometric baseline (Spherical Spline: MAE $\approx$ \ResBaselineMAE, SNR $\approx$ \ResBaselineSNR~dB). Tikhonov regularization also performed robustly (MAE $\approx$ \ResGSPMAE, SNR $\approx$ \ResGSPSNR~dB).

\begin{figure}[ht]
  \centering
  \includegraphics[width=\linewidth]{../../results_optuna_final/global_comparison_physionet.png}
  \caption{Global performance comparison on the PhysioNet dataset after Optuna optimization. Temporal regularization methods (TRSS, Tikhonov) dominate the top rankings.}
  \label{fig:global_physionet}
\end{figure}

\subsubsection{Spatial and Temporal Fidelity}
Figure~\ref{fig:radar_mne} presents a multi-metric radar chart for the MNE Sample dataset. It illustrates that TRSS not only minimizes spatial errors (MAE, RMSE) but also achieves superior temporal fidelity (DTW $\approx$ \ResTVDTW), a critical factor for EEG applications such as Event-Related Potentials (ERP) analysis.

\begin{figure}[ht]
  \centering
  \includegraphics[width=\linewidth]{../../results_optuna_final/radar_multimetric_mne_sample_random_0.4.png}
  \caption{Radar chart showing multi-metric performance on the MNE Sample dataset under a 40\% random missing channel scenario. TRSS maximizes performance across all axes.}
  \label{fig:radar_mne}
\end{figure}

\subsubsection{Robustness to Degradation}
Figure~\ref{fig:degradation_bci} shows the degradation curve for the BCI IV 2a dataset as the missing channel ratio increases from 10\% to 40\%. TRSS maintains an acceptable signal-to-noise ratio even at high missing rates, whereas classical baselines degrade rapidly. The spatial configuration of the missing channels (\emph{nearby} vs. \emph{random}) strongly affects all methods, but temporal regularization mitigates the catastrophic failure seen in purely spatial methods when entire electrode clusters are lost.

\begin{figure}[ht]
  \centering
  \includegraphics[width=\linewidth]{../../results_optuna_final/degradation_bci_iv_random_ratio.png}
  \caption{SNR degradation curve for the BCI IV 2a dataset under increasing random missing channel ratios. TRSS demonstrates superior robustness.}
  \label{fig:degradation_bci}
\end{figure}

In summary, the optimized results confirm that combining graph-based spatial topologies with temporal smoothness priors yields a highly effective and robust EEG reconstruction framework, validating the core hypothesis of this work.

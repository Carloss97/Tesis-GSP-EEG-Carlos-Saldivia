\section{Conclusion}

Graph-based interpolation is a strong candidate for EEG reconstruction under missing-channel settings, especially when graph construction and protocol design are controlled.

Future work should finalize strict replication checks and extend robustness analyses across datasets and masking regimes.

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish,es-tabla]{babel}
\usepackage[letterpaper,left=1.2in,right=1in,top=1in,bottom=1in]{geometry}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{setspace}
\usepackage{fancyhdr}
\usepackage{hyperref}
% Provide a robust code macro to use in moving arguments. Use \code{...}
% when you need literal rendering (underscores, etc.). \code wraps the
% original \texttt but detokenizes the content so it is safe in captions/TOC.
\DeclareRobustCommand{\code}[1]{\texttt{\detokenize{#1}}}
% Ensure PDF string conversion ignores formatting used in captions (e.g., \texttt)
% and in the custom \code macro. Define this after \code so both are covered.
\pdfstringdefDisableCommands{%
	\def\texttt#1{#1}%
	\def\code#1{#1}%
}
\usepackage{url}
\usepackage{caption}
\usepackage{subcaption}

% Reduce table column separation and use smaller font in table floats
% to mitigate extreme overfull boxes from auto-generated wide tables.
\usepackage{etoolbox}
\AtBeginEnvironment{table}{\scriptsize\setlength{\tabcolsep}{1.5pt}}
\renewcommand{\arraystretch}{0.88}

\renewcommand{\baselinestretch}{1.2}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\cfoot{\thepage}




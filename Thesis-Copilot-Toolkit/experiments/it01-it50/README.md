# it01-it50 — Agrupación de iteraciones

Carpeta de agrupación para identificar rápidamente las iteraciones generadas it01–it50.

Contenido y referencias:

- Schedule completo (JSON): `../schedules/it01-it50_schedule.json`
- Light-profile (pilot): `../schedules/it01-it50_light_profile.json`
- Módulo generador: `../iterdef.py`
- Script de generación: `../generate_schedule_it01_it50.py`

Uso rápido:

1. Revisar los schedules en `Thesis-Copilot-Toolkit/experiments/schedules/`.
2. Ejecutar iteraciones con los scripts de runner correspondientes, p.ej.:

```bash
python Thesis-Copilot-Toolkit/experiments/run_future_work_it121_it130.py --tags it17 it48 --light-profile
```

Notas:

- Los archivos JSON en `experiments/schedules/` son auto-generados y pueden regenerarse con `generate_schedule_it01_it50.py`.
- Esta carpeta es solo un índice local para facilitar la navegación; los artefactos de ejecución se escriben en `Thesis-Copilot-Toolkit/results/`.

--
Generado por la herramienta de planificación de iteraciones.
